blueprint:
name: Flexibele Aan/Uit Automatisering
description: |
Definieer verschillende triggers voor AAN en UIT acties.
Volledig flexibel voor tijd, sensoren, devices, etc.
domain: automation
input:
on_trigger:
name: AAN Trigger
description: Wanneer moeten de AAN acties uitgevoerd worden?
selector:
object: {}
on_actions:
name: AAN Acties
description: Acties die uitgevoerd worden bij de AAN trigger.
selector:
action: {}
off_trigger:
name: UIT Trigger  
description: Wanneer moeten de UIT acties uitgevoerd worden?
selector:
object: {}
off_actions:
name: UIT Acties
description: Acties die uitgevoerd worden bij de UIT trigger.
selector:
action: {}

trigger:

- id: “on”
  <<: !input on_trigger
- id: “off”  
  <<: !input off_trigger

condition: []

action:

- choose:
  - conditions:
    - condition: trigger
      id: “on”
      sequence: !input on_actions
  - conditions:
    - condition: trigger
      id: “off”
      sequence: !input off_actions

mode: single